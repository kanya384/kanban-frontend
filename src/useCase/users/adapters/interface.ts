import { User } from "../../../domain/user/user";

export interface UsersInterface {
    /*ReadUsers(): Promise<User[] | Error>
    ReadUserById(id: UniqueId): Promise<User | Error>
    CreateUser(name: string, login: string, pass: string, accessList: string[]): Promise<User | Error>
    UpdateUser(id: UniqueId, name: string, login: string, pass: string, accessList: string[]): Promise<User | Error>
    DeleteUser(id: UniqueId): Promise<string | Error>*/
}